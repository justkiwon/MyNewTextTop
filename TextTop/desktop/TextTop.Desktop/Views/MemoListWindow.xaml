<Window x:Class="TextTop.Desktop.Views.MemoListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextTop Memos" Width="420" Height="560"
        WindowStartupLocation="CenterScreen" Background="#F3F4F6">
    <Border Margin="18" Padding="18" CornerRadius="16" Background="White">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.14"/>
        </Border.Effect>
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,14">
                <TextBlock Text="TextTop" FontSize="28" FontWeight="SemiBold" Foreground="#111827"/>
                <TextBlock Text="저장된 제목을 선택해서 메모를 Load합니다." Foreground="#6B7280" Margin="0,2,0,14"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Height="34" Padding="12,0" Background="#FFFFFF" BorderBrush="#D1D5DB"/>
                    <Button Grid.Column="1" Content="Load" Command="{Binding OpenCommand}" Height="34" Padding="16,0" Margin="8,0,0,0" Background="#2563EB" Foreground="White" BorderThickness="0"/>
                    <Button Grid.Column="2" Content="New" Command="{Binding NewCommand}" Height="34" Padding="14,0" Margin="8,0,0,0" Background="#111827" Foreground="White" BorderThickness="0"/>
                    <Button Grid.Column="3" Content="Close" Height="34" Padding="12,0" Margin="8,0,0,0" Click="CloseButton_Click"/>
                </Grid>
            </StackPanel>

            <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusText}" Foreground="#6B7280" FontSize="12" TextWrapping="Wrap" Margin="0,10,0,0"/>

            <ListBox ItemsSource="{Binding Memos}" SelectedItem="{Binding SelectedMemo}" MouseDoubleClick="MemoList_DoubleClick"
                     BorderBrush="#E5E7EB" Background="#F9FAFB" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" Margin="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="15" Foreground="#111827" TextTrimming="CharacterEllipsis"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="Version " Foreground="#6B7280" FontSize="12"/>
                                    <TextBlock Text="{Binding Version}" Foreground="#6B7280" FontSize="12"/>
                                    <TextBlock Text="  |  " Foreground="#D1D5DB" FontSize="12"/>
                                    <TextBlock Text="{Binding UpdatedAt}" Foreground="#6B7280" FontSize="12"/>
                                </StackPanel>
                                <TextBlock Text="{Binding SyncState}" Foreground="#9CA3AF" FontSize="12" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
