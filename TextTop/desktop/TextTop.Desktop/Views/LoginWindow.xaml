<Window x:Class="TextTop.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextTop Login" Width="420" Height="480"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F3F4F6">
    <Grid>
        <Border Width="320" Padding="28" CornerRadius="18" Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.18"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="TextTop" FontSize="30" FontWeight="SemiBold" Foreground="#111827" HorizontalAlignment="Center"/>
                <TextBlock Text="Cloud Memo" FontSize="14" Foreground="#6B7280" HorizontalAlignment="Center" Margin="0,4,0,28"/>
                <TextBlock Text="Email" Foreground="#374151" Margin="0,0,0,6"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="38" Padding="10,0" VerticalContentAlignment="Center"/>
                <TextBlock Text="Password" Foreground="#374151" Margin="0,14,0,6"/>
                <PasswordBox x:Name="PasswordInput" Height="38" Padding="10,0" VerticalContentAlignment="Center"/>
                <CheckBox Content="Remember email/password" IsChecked="{Binding RememberLogin}" Margin="0,12,0,4"/>
                <CheckBox Content="Auto login and refresh token" IsChecked="{Binding AutoLogin}"/>
                <Button Content="Login" Height="40" Margin="0,18,0,14" Background="#2563EB" Foreground="White" BorderThickness="0" Click="LoginButton_Click"/>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#4B5563" TextWrapping="Wrap" TextAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
